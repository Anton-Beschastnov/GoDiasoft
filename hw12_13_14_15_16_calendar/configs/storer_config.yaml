logger:
  level: "info"

database:
  host: "localhost"
  port: 5432
  user: "calendar"
  password: "calendar"
  database: "calendar"

kafka:
  bootstrap_servers:
    - "localhost:9092"
  topic: "calendar_notifications"

storer:
  kafka_topic: "calendar_notifications"